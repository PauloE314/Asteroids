/**
 * @param {Number} a
 * @param {Number} b
 * @returns {Number}
 */
export function getSmaller(a, b) {
    return a < b ? a : b;
}

/**
 * @param {Number} n
 */
export function radian(n) {
    return (Math.PI / 180) * n;
}
